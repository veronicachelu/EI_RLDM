from setuptools import setup, find_packages

setup(
    name="rnn_lib",
    version="0.1",
    packages=find_packages(),  # Automatically finds `rnn_lib`
    install_requires=[],  # Add dependencies here if needed
)
